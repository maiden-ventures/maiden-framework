- name: test
  api: true
  admin: true
  columns:
    - name: first_name
      type: varchar
      limit: 255
      index: true

- name: Test2
  columns:
    - name: fname
      type: varchar
      limit: 100
      references:
        table: test
        column: first_name
        on_delete: cascade
