
def update(data: @@model@@) = {
  val d = data.copy(updatedAt = Option(LocalDateTime.now))
  val q = quote {
    @@queryName@@.filter(_.id == lift(d.id)).update
  }

  execute(q)(List(d))
  get(d.id)

}