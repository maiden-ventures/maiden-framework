def create(data: @@model@@) = {
  val q = quote {
    @@queryName@@.insert
  }
  db.run(q)(List(data)).headOption match {
    case Some(id) => get(id)
    case _ => throw(new Exception("unable to create model"))
  }
}