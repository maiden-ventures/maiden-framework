
def findBy[T](col: String, value: T) ={
  val q = col match {
      @@findByCase@@

      case _ => throw(new Exception(s"Unknown column ${col}"))
  }
  db.run(q).map(x => @@model@@FullResponse.build(x))
}